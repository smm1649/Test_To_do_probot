% @todo create an issue for testing
% @body check if that is working

function c = addMe(a,b)
% ADDME addign two value
% C = ADDME(A) adds A to itself
% C = ADDME(A,B) adds A and B togather
% See also SUM, PLUS

	c = a + b
end
